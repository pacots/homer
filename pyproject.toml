[project]
name = "python-ai-agent"
version = "0.1.0"
description = "Template structure for a Python AI agent project."
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]

