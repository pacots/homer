# Python AI agent template (structure only)

```
python-ai-agent/
├── pyproject.toml
├── agent.toml
├── src/
│   └── agent/
│       └── .gitkeep
├── prompts/
│   └── .gitkeep
├── memory/
│   └── .gitkeep
└── tests/
    └── .gitkeep
```

